#!/usr/bin/env variant
# vi: ft=yaml

tasks:
  core:
    import: github.com/aroq/toolbox?ref=v2.1//variant-lib/utils.yaml
  config:
    import: github.com/aroq/toolbox?ref=v2.1//variant-lib/utils.config.yaml

tasks:
  process:
    steps:
    - task: config.config.process.dir
