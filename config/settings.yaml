config:
  libraries:
    params:
      prefix: .toolbox
      tools_dir: tools
      image: aroq/toolbox
      env:
        TOOLBOX_RUN_VARIANT: true
        TOOLBOX_DOCKER_SSH_FORWARD: false
    toolbox:
      params:
        source: github.com/aroq/toolbox//tools
        cmd: toolbox/toolbox
    k6:
      params:
        source: github.com/aroq/toolbox-k6
        image: aroq/toolbox-k6
      commands:
        k6:
          params:
            cmd: k6/k6
    cicd:
      params:
        source: github.com/aroq/toolbox-cicd
        image: aroq/variant-cicd
        env:
          TOOLBOX_DOCKER_SSH_FORWARD: true
      commands:
        release:
          build:
            params:
              cmd: release/build/build
          deploy:
            params:
              cmd: release/deploy
tools:
  toolbox:
    from: config.libraries.toolbox
